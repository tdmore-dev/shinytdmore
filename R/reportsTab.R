#'
#' Get the reports tab panel.
#'
#' @return a panel
#'
getReportsTabPanel <- function() {
  panel <- tabPanel("Reports",
                    icon = icon("file-text"),
                    HTML("Reports tab under construction.")
                    )
  return(panel)
}